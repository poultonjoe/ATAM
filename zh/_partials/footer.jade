- var lang = current.path[0] === 'zh' ? 'zh' : '';
- var data = lang !== 'zh' ? public._data : public.zh._data;
- var navigation = data.navigation;
- var year = (new Date()).getFullYear();
- var baseUrl = github_pages ? (environment === 'development' ? '' : '/ATAM') : '';

footer#site-footer(role='contentinfo')
  .wrap
    div.info
      div.copyright AT Asset Management &copy; #{year}.
      div.registration Registration Number 201212588D.

    nav.footer-navigation
      ul
        - each page in navigation
          - var href = (lang ? lang + '/' : '') + page.slug
          li: a(href='#{baseUrl}/#{href}/', title='#{page.title}')= page.title
        - each page in data.footer_navigation
          - var href = (lang ? lang + '/' : '') + page.slug
          li: a(href='#{baseUrl}/#{href}/', title='#{title}')= page.title